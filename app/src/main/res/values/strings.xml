<resources>
    <string name="app_name">MeetMind</string>
    <!--
      Firebase/Google Sign-In "Web client ID" (OAuth client, type "Web application") from the
      Firebase console — this is a public identifier, not a secret, and is safe to commit.
      Leave this blank to build and run fully offline with Google Sign-In reported as
      unavailable rather than fabricating a signed-in user. See docs/AI_ARCHITECTURE.md and
      docs/ARCHITECTURE.md for how authentication degrades when this (or google-services.json)
      is not configured.
    -->
    <string name="google_sign_in_web_client_id" translatable="false"></string>
</resources>
