<?xml version="1.0" encoding="utf-8"?>
<!--
  Legacy Auto Backup rules for API < 31 (ignored on API 31+, which uses data_extraction_rules.xml
  instead — kept in sync with it). See that file's comment for why recordings/transcripts are
  excluded from backup.
-->
<full-backup-content>
    <exclude domain="file" path="meetings/" />
    <exclude domain="database" path="meetmind_database" />
</full-backup-content>
